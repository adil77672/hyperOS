# Copy to .env.local.
NEXT_PUBLIC_API_BASE=http://localhost:3000

# Platform root domain. Store subdomains are {slug}.{root}; the bare root is the
# marketplace; `admin.{root}` is the merchant/super-admin surface.
NEXT_PUBLIC_ROOT_DOMAIN=hyperos.co
